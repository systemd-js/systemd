{
  "schedule": ["every weekend"],
  "rangeStrategy": "bump",
  "extends": [
    "config:base"
  ], 
  "packageRules": [
    {
      "matchPackageNames": ["find-up"],
      "enabled": false,
      "labels": ["ESM not compact with ESLint"]
    }
  ]
}
